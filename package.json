{
  "name": "crawlcast-extension",
  "version": "0.4.0",
  "private": true,
  "scripts": {
    "prepare": "husky",
    "commit": "cz",
    "test": "node --test tests/*.test.cjs"
  },
  "config": {
    "commitizen": {
      "path": "cz-conventional-changelog"
    }
  },
  "devDependencies": {
    "@commitlint/cli": "^21.2.2",
    "@commitlint/config-conventional": "^21.2.2",
    "commitizen": "^4.3.2",
    "cz-conventional-changelog": "^3.3.0",
    "husky": "^9.1.7"
  }
}
